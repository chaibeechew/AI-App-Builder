"use client";

export default function GeneratedAppError({reset}){
  return <main className="premiumRouteState"><div className="stateScene error"><span>!</span><small>YOUR WORK IS SAFE</small><h1>This Preview needs another try</h1><p>We could not open the App just now. Nothing was published or removed.</p><button onClick={()=>reset()}>Try Preview Again</button></div></main>;
}
