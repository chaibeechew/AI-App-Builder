import Link from "next/link";
import { redirect } from "next/navigation";
import { createClient } from "../../../lib/supabase/server.js";

export default async function AppDashboard({ params }) {
  const { id } = await params;
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/auth");
  const { data: app, error } = await supabase.from("apps").select("id,name,description,created_at,updated_at,current_version_id,visibility,publish_status").eq("id", id).eq("owner_id", user.id).single();
  if (error || !app) redirect("/my-apps");
  const { data: versions } = await supabase.from("app_versions").select("id,version_no,specification,change_summary,created_at").eq("app_id", id).order("version_no", { ascending: false });
  const current = versions?.find(v => v.id === app.current_version_id) || versions?.[0];
  const spec = current?.specification || {};
  const pages = Array.isArray(spec.pages) ? spec.pages : [];
  const features = Array.isArray(spec.features) ? spec.features : [];
  return <main className="page"><div className="wrap"><Link href="/my-apps" className="back">← My Apps</Link><header><div><div className="eyebrow">APP DASHBOARD</div><h1>{app.name}</h1><p>{app.description || "Your application workspace."}</p></div><span className="status">{app.publish_status === "published" ? "Published" : "Draft"}</span></header><section className="stats"><div><small>Version</small><strong>{current?.version_no || 1}</strong></div><div><small>Pages</small><strong>{pages.length}</strong></div><div><small>Features</small><strong>{features.length}</strong></div></section><section className="card"><div className="eyebrow">APP FOLDER</div><h2>Manage this app</h2><div className="actions"><Link href={`/editor/${id}`} className="primary">Modify</Link><Link href={`/publish/${id}`} className="secondary">Publish</Link></div><div className="flow"><span>Preview</span><i>→</i><span>Modify</span><i>→</i><span>Test</span><i>→</i><span>Publish</span><i>→</i><span>Rollback</span></div></section><section className="card"><div className="eyebrow">CURRENT VERSION</div><h2>Application structure</h2>{pages.length ? pages.map((p,i)=><div className="row" key={i}><strong>{p?.name || `Page ${i+1}`}</strong><span>{p?.purpose || "Application page"}</span></div>) : <p>No page structure available yet.</p>}</section><section className="card"><div className="eyebrow">SOURCE CODE</div><h2>Customer source access</h2><p>Source Code remains inside the app's protected Hidden Folder. Server-side permissions decide access; the UI alone is never the security boundary.</p><div className="protected">🔐 Protected by server-side access control</div></section><footer><Link href={`/editor/${id}`}>Open / Modify</Link><Link href="/my-apps">Back to My Apps</Link></footer></div><style>{`.page{min-height:100vh;padding:36px 18px 80px;background:linear-gradient(145deg,#03100d,#0a2119 58%,#06140f);color:#f5fff9}.wrap{max-width:1050px;margin:auto}.back,footer a{color:#d8bf62;text-decoration:none}.eyebrow{color:#d8bf62;letter-spacing:.2em;font-size:11px;font-weight:900}.page header{display:flex;justify-content:space-between;gap:20px;align-items:flex-start;margin:22px 0 26px}.page h1{font-size:46px;margin:8px 0}.page p{color:#93aaa0;line-height:1.6}.status{color:#79d7ac;border:1px solid rgba(121,215,172,.2);border-radius:999px;padding:9px 13px;font-size:12px;font-weight:800}.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:16px}.stats div,.card{border:1px solid rgba(255,255,255,.08);background:rgba(3,16,13,.78);border-radius:20px;padding:20px}.stats small,.stats strong{display:block}.stats small{color:#7f9990;font-size:11px}.stats strong{font-size:28px;margin-top:5px}.card{margin-top:16px}.card h2{margin:8px 0 18px}.actions{display:flex;gap:10px;flex-wrap:wrap}.primary,.secondary{display:inline-flex;padding:12px 16px;border-radius:12px;font-weight:800;text-decoration:none}.primary{background:#d8bf62;color:#07130e}.secondary{border:1px solid rgba(216,191,98,.25);color:#d8bf62}.flow{display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-top:20px;color:#a9bbb4}.flow span{padding:8px 11px;border-radius:10px;background:#0e3024;color:#d8bf62}.row{display:grid;grid-template-columns:220px 1fr;gap:18px;padding:14px 0;border-top:1px solid rgba(255,255,255,.06)}.row span{color:#8ea69c}.protected{margin-top:14px;padding:12px;border-radius:12px;background:rgba(70,190,140,.08);color:#8de0bb}footer{display:flex;justify-content:space-between;margin-top:24px}@media(max-width:700px){.stats{grid-template-columns:1fr}.page header{flex-direction:column}.row{grid-template-columns:1fr}footer{flex-direction:column;gap:12px}}`}</style></main>;
}
