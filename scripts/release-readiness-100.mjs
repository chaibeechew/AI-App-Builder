import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();
const read=(p)=>fs.existsSync(path.join(root,p))?fs.readFileSync(path.join(root,p),'utf8'):'';
const exists=(p)=>fs.existsSync(path.join(root,p));
const contains=(p,terms=[])=>terms.every(term=>read(p).includes(term));

const publish=read('app/api/apps/[id]/publish/route.js');const quality=read('app/api/apps/[id]/quality/route.js');const storePublish=read('app/api/publish/request/route.js');const policy=read('config/product-policy.js');const editor=read('app/editor/[id]/page.js');const dashboard=read('app/app-dashboard/[id]/page.js');const proPage=read('app/pro/[id]/page.js');const proAssistant=read('app/pro/[id]/ProAssistant.js');const accessReader=read('lib/app-builder-access.js');const accessRuntime=read('supabase/migrations/20260831120000_preview_access_credit_runtime.sql');const creditsApi=read('app/api/credits/route.js');const releasePage=read('app/release/[id]/page.js');const auth=read('app/auth/page.js');const modify=read('app/api/modify/route.js');const generate=read('app/api/generate/route.js');const autonomous=read('engine/autonomous-engine.js');const workflow=read('.github/workflows/consolidation-ci.yml');const workflowRun=read('app/api/apps/[id]/workflows/[workflowId]/run/route.js');const checkout=read('app/api/apps/[id]/monetization/[offerId]/checkout/route.js');const integrationServer=read('lib/integrations/server.js');const databaseApi=read('app/api/apps/[id]/database/route.js');const databaseRollback=read('app/api/apps/[id]/database/rollback/route.js');const videoCompile=read('app/api/video/projects/[id]/compile/route.js');const readiness=read('lib/release-readiness.js');const nonProductionReadiness=read('lib/non-production-readiness.js');const buildStandards=read('lib/buildStandards.js');const runtimeGuard=read('lib/generator/runtime-guard.js');const videoApiIndex=read('app/api/video/projects/route.js')+read('app/api/video/storyboard/route.js')+videoCompile;const releaseTests=read('scripts/release-policy-tests.mjs');const securityTests=read('scripts/security-contract-tests.mjs');const runtimeTests=read('scripts/runtime-contract-tests.mjs');const nonProductionTests=read('scripts/non-production-100-tests.mjs');const visualPolicy=read('lib/ai/premium-visual-policy.js');const wallpaperPresets=read('lib/design/wallpaper-presets.js');const wallpaperEngine=read('app/components/AdaptiveWallpaperEngine.js');const imageStudio=read('app/image-studio/page.js');const imageApi=read('app/api/images/generate/route.js');const generatedApp=read('app/a/[id]/GeneratedAppClient.js');const generatedWebsite=read('app/website/[id]/page.js');const layout=read('app/layout.js');const productCopy=read('app/components/ProductCopyFix.js');const projectMemory=read('lib/project-memory.js');const studio=read('app/studio/page.js');

const checks=[
['Core build route persists generated projects',contains('app/api/generate/route.js',['app_versions','specification'])],
['Generation reserves access by request and restores failed creation',generate.includes('p_request_id:chargeRequestId')&&generate.includes('bind_app_builder_project_access')&&generate.includes('restore_failed_app_builder_create')],
['AI modify route versions changes',modify.includes('app_versions')&&modify.includes('version_no')],
['AI modify uses request-bound entitlement checks',modify.includes('p_request_id:chargeRequestId')],
['AI modify applies same generation quality standard',modify.includes('GENERATION_QUALITY_RULES')&&modify.includes('assessBuildQuality')],
['AI modify preserves explicit quality evidence',modify.includes('qualityPlan')&&modify.includes('at least 3 concrete implementation decisions')],
['AI modify repairs deterministic quality regressions',modify.includes('qualityRegressed')&&modify.includes('Quality Repair')],
['AI modify fails closed if repair still regresses quality',modify.includes('would reduce the project')&&modify.includes('status:409')],
['No-code editor uses natural-language AI modify',editor.includes('/api/modify')&&editor.includes('textarea')],
['No-code editor preserves version history access',editor.includes('Version History')&&editor.includes('Rollback')],
['No-code editor supports customer colors and wallpaper selection',editor.includes('type="color"')&&editor.includes('WALLPAPER_PRESETS')&&editor.includes('COLOR & WALLPAPER')],
['Project dashboard keeps simple preview/change/publish actions',['Open App Demo','Preview Website','Publishing'].every(term=>dashboard.includes(term))],
['Professional Mode is visible from the project dashboard',dashboard.includes(`/pro/${'${id}'}`)&&exists('app/pro/[id]/page.js')],
['Professional Mode keeps AI-first natural-language editing',proAssistant.includes('/api/modify')&&(proPage.includes('Professional Workspace')||proPage.includes('Advanced control with AI assistance'))],
['Professional tools are gated by server-side 365-day access',proPage.includes('getAppBuilderAccess')&&proPage.includes('!access.professional.active')&&accessReader.includes('pro_valid_until')],
['Standard and Pro grants are server-only financial state',accessRuntime.includes('standard_project_credits')&&accessRuntime.includes('grant_standard_project_credit')&&accessRuntime.includes('grant_pro_access')&&accessRuntime.includes('to service_role')],
['AI credit charge/refund runtime is idempotent and amount-bound',accessRuntime.includes('credit_transactions_request_type_unique_idx')&&accessRuntime.includes('Refund amount does not match original charge')&&accessRuntime.includes("type = 'ai_usage'")],
['Credits API reads the consolidated protected ledger',creditsApi.includes('credit_transactions')&&creditsApi.includes('.maybeSingle()')&&!creditsApi.includes('credit_ledger')],
['Central release policy exists at 100',contains('lib/release-readiness.js',['RELEASE_SCORE_REQUIRED = 100','evaluateReleaseReadiness','evaluateProductionEvidence','PRODUCTION_EVIDENCE_REQUIREMENTS'])],
['Non-production readiness has its own strict 100-point model',nonProductionReadiness.includes('NON_PRODUCTION_SCORE_REQUIRED = 100')&&nonProductionReadiness.includes('productionHeld: true')],
['Non-production model covers all important product areas',['generation','editing','data','automation','publishing','security','reliability','visual','wallpaper','imageStudio','versioning','pro','branding'].every(k=>nonProductionReadiness.includes(`key: "${k}"`))],
['Platform Production promotion is explicitly held',policy.includes('productionPromotionHold: true')&&policy.includes('explicitApprovalRequiredBeforeProduction: true')&&workflow.includes('Production promotion remains on hold')],
['Canonical product brand is AI BUILD APP & WEB',policy.includes('product: "AI BUILD APP & WEB"')&&layout.includes('PRODUCT_BRAND.name')&&productCopy.includes('AI BUILD APP & WEB')],
['Premium visual policy makes customer color choice authoritative',visualPolicy.includes('customer color preference is authoritative')&&policy.includes('customerColorChoiceAuthoritative: true')],
['Step wallpaper library and adaptive engine exist',wallpaperPresets.includes('WALLPAPER_PRESETS')&&wallpaperPresets.includes('pickWallpaperForStage')&&wallpaperEngine.includes('MutationObserver')&&wallpaperEngine.includes('AI Random')],
['Wallpaper preference is stored and carried into generation',wallpaperEngine.includes('ai-build-wallpaper')&&generate.includes('wallpaperMode')&&generate.includes('visual_preferences')],
['Generated App and Website render saved wallpaper direction',generatedApp.includes('wallpaperStyle')&&generatedWebsite.includes('wallpaperStyle')],
['Project memory preserves visual preferences',projectMemory.includes('visualPreferences')&&projectMemory.includes('Visual preferences')],
['Image Studio supports multi-direction design workflow',imageStudio.includes('Design Images')&&imageStudio.includes('Style Tools')&&imageStudio.includes('Templates')&&imageApi.includes('images')],
['Image engine is truthful about current renderer',imageApi.includes('Soolen Visual Engine')&&imageApi.includes('not presented as photorealistic external-model output')],
['Studio links Image Studio and avoids unsupported template count claims',studio.includes('/image-studio')&&!studio.includes('3,000+')],
['Quality API uses shared release evaluator',quality.includes('evaluateReleaseReadiness')],
['Quality API exposes real-world evidence requirements',quality.includes('productionEvidence')&&quality.includes('PRODUCTION_EVIDENCE_LABELS')],
['Publish Center explains real-world evidence separately from score',releasePage.includes('REAL-WORLD RELEASE EVIDENCE')&&releasePage.includes('100/100 does not replace live testing')],
['Website/App publish API uses shared release evaluator',publish.includes('evaluateReleaseReadiness')],
['Store publish request uses exact current version and shared evaluator',storePublish.includes('current_version_id')&&storePublish.includes('evaluateReleaseReadiness')&&storePublish.includes('customer_approved_at')],
['Publish route blocks below strict target',publish.includes('Publishing is locked until the project reaches')&&publish.includes('releaseReady: false')],
['All six dimensions are centrally required',['stability','security','privacy','comfort','beauty','naturalness'].every(k=>readiness.includes(`"${k}"`))],
['100 is described as internal target not zero-defect guarantee',readiness.includes('not a guarantee of zero bugs')],
['Production evidence fails closed by contract',releaseTests.includes('Production evidence must fail closed when missing')&&releaseTests.includes('Complete production evidence should pass')],
['Generation receives explicit quality rules',autonomous.includes('GENERATION_QUALITY_RULES')&&generate.includes('runAutonomousEngine')],
['Generation emits explicit qualityPlan evidence',autonomous.includes('QUALITY PLAN REQUIREMENT')&&autonomous.includes('"qualityPlan"')],
['Generation records theme and wallpaper direction',autonomous.includes('wallpaperPreset')&&autonomous.includes('paletteRationale')&&autonomous.includes('CUSTOMER THEME DIRECTION')],
['Perfect score cannot come from keywords alone',buildStandards.includes('evidenceComplete')&&buildStandards.includes('Math.min(99,raw)')&&buildStandards.includes('qualityPlan')],
['Runtime guard preserves quality and visual metadata',['qualityPlan','backgroundTreatment','dataModels','demoVideo'].every(term=>runtimeGuard.includes(term))],
['Premium visual direction is generated',contains('engine/autonomous-engine.js',['designSystem','backgroundDirection','heroDirection','layoutSignature'])],
['Fair Price Fair Use customer policy exists',policy.includes('Fair Price · Fair Use')],
['Free first App + Website continues until first publish',policy.includes('freeFirstProject')&&policy.includes('includesReasonableAiModificationUntilReady: true')&&policy.includes('endsWhenProjectIsPublished: true')],
['Standard remains USD 10 one-time',policy.includes('priceUsd: 10')&&policy.includes('billing: "one_time"')],
['Pro remains USD 68 for 365 days without auto-renew',policy.includes('priceUsd: 68')&&policy.includes('accessDays: 365')&&policy.includes('autoRenew: false')],
['Price review is at three years and increase optional',policy.includes('reviewIntervalYears: 3')&&policy.includes('increaseIsOptional: true')],
['License/buyout plan remains available',contains('config/product-policy.js',['oneAppOneLicense','personal: { priceUsd: 49 }','business: { priceUsd: 199 }','enterprise: { priceUsd: 499 }'])],
['Apple/Google developer fees are never collected by platform',policy.includes('chargedByAiAppBuilder: false')&&policy.includes('collectedByAiAppBuilder: false')],
['Store publishing requires customer review',policy.includes('customerMustReviewBeforeSubmission: true')],
['Store metadata assistant exists',exists('app/api/store-metadata/route.js')&&exists('app/publish/[id]/page.js')],
['Email OTP path exists',auth.includes('signInWithOtp')&&auth.includes('verifyOtp')],
['SMS remains safely feature-flagged while provider is paused',auth.includes('NEXT_PUBLIC_SMS_AUTH_ENABLED')],
['Database builder validates types and credential fields',databaseApi.includes('SAFE_TYPES')&&databaseApi.includes('SECRET_FIELD')],
['Database builder and UI provide non-destructive rollback',databaseApi.includes('_history')&&exists('app/api/apps/[id]/database/rollback/route.js')&&databaseRollback.includes('newVersion')&&read('app/database/[id]/page.js').includes('Restore this version')],
['Workflow automation is idempotent and bounded',workflowRun.includes('idempotency_key')&&workflowRun.includes('Workflow action timed out')&&workflowRun.includes('criticalFailure')],
['Managed providers use bounded network timeouts',integrationServer.includes('External provider timed out')&&integrationServer.includes('providerFetch')],
['Payments use server-side offer validation and provider idempotency',checkout.includes('Offer amount is outside the supported range')&&checkout.includes('idempotencyKey')&&integrationServer.includes('Idempotency-Key')],
['Integration center exists',exists('app/integrations/[id]/page.js')],
['Analytics and AI operations exist',exists('app/analytics/[id]/page.js')&&exists('app/operations/[id]/page.js')],
['Video Studio is present without pretending provider completion',exists('app/video-studio/page.js')&&videoCompile.includes('renderStarted:false')&&videoCompile.includes('rendererConfigured')&&!/final provider connected|fully connected video provider|guaranteed mp4/i.test(videoApiIndex)],
['Release policy regression tests cover 100-point fail-closed behavior',releaseTests.includes('Any quality dimension below 100 must fail')&&releaseTests.includes('Missing dimensions must fail closed')],
['Security contract tests cover ownership and client secrets',securityTests.includes('Client bundles must never reference server secrets')&&securityTests.includes('Publish must enforce project ownership server-side')],
['Runtime contract tests cover workflow, payment, store, database and video',['Workflow execution is idempotent','Payment checkout uses authoritative','Store publish requests require exact-version','No-code database models','Video compile does not pretend'].every(term=>runtimeTests.includes(term))],
['Non-production regression test covers product readiness while Production stays held',nonProductionTests.includes('Non-production 100-point contract')&&nonProductionTests.includes('Production')],
['CI runs release tests before security tests',workflow.indexOf('npm run test:release')>=0&&workflow.indexOf('npm run test:release')<workflow.indexOf('npm run test:security')],
['CI runs security tests before runtime tests',workflow.indexOf('npm run test:security')>=0&&workflow.indexOf('npm run test:security')<workflow.indexOf('npm run test:runtime')],
['CI runs runtime tests before non-production tests',workflow.indexOf('npm run test:runtime')>=0&&workflow.indexOf('npm run test:runtime')<workflow.indexOf('npm run test:nonprod')],
['CI runs non-production tests before 100 readiness gate',workflow.indexOf('npm run test:nonprod')>=0&&workflow.indexOf('npm run test:nonprod')<workflow.indexOf('npm run quality:100')],
['CI runs 100 readiness gate before build',workflow.indexOf('npm run quality:100')>=0&&workflow.indexOf('npm run quality:100')<workflow.indexOf('npm run build')],
['CI builds exact branch',workflow.includes('integration/primary-consolidation')],
['CI contains no automatic Production deployment step',!/vercel\s+--prod|vercel\s+deploy\s+--prod|production-deploy|promote-to-production/i.test(workflow)],
['No fake security or zero-bug marketing claims in quality policy',!/guaranteed security|100% secure|zero bugs guaranteed/i.test(buildStandards)],
];

const failed=checks.filter(([,ok])=>!ok);const passed=checks.length-failed.length;const score=Math.round((passed/checks.length)*100);console.log(`Platform repository readiness: ${score}/100 (${passed}/${checks.length})`);for(const[name,ok]of checks)console.log(`${ok?'✓':'✗'} ${name}`);if(failed.length){console.error(`\nRelease blocked: ${failed.length} repository readiness check(s) failed.`);process.exit(1)}console.log('\nRepository 100 gate passed for product/code readiness. Platform Production promotion remains intentionally held and still requires explicit approval plus live evidence.');
