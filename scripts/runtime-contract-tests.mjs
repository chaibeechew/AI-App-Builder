import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd(),read=p=>fs.readFileSync(path.join(root,p),'utf8');
const workflow=read('app/api/apps/[id]/workflows/[workflowId]/run/route.js'),workflowPage=read('app/workflows/[id]/page.js'),checkout=read('app/api/apps/[id]/monetization/[offerId]/checkout/route.js'),integrations=read('lib/integrations/server.js'),storePublish=read('app/api/publish/request/route.js'),database=read('app/api/apps/[id]/database/route.js'),databaseRollback=read('app/api/apps/[id]/database/rollback/route.js'),projectRollback=read('app/api/apps/[id]/rollback/route.js'),dataRecords=read('app/api/apps/[id]/records/route.js'),dataRecordsMigration=read('supabase/migrations/20260831174000_add_app_data_records.sql'),generatedApp=read('app/a/[id]/GeneratedAppClient.js'),videoCompile=read('app/api/video/projects/[id]/compile/route.js'),videoGateway=read('lib/video/render-gateway.js'),videoStatus=read('app/api/video/projects/[id]/versions/[versionId]/route.js'),imageApi=read('app/api/images/generate/route.js'),imageGateway=read('lib/ai/image-generation-gateway.js'),generate=read('app/api/generate/route.js'),modify=read('app/api/modify/route.js'),proAssistant=read('app/pro/[id]/ProAssistant.js'),proPage=read('app/pro/[id]/page.js'),bootstrap=read('app/api/apps/[id]/bootstrap/route.js'),modifyRuntime=read('supabase/migrations/20260831181000_harden_professional_modify_runtime.sql'),finance=read('lib/app-builder-finance.js'),serverRpc=read('supabase/migrations/20260831170000_server_only_entitlements_and_credits.sql');
assert.match(workflow,/idempotency_key/);assert.match(workflow,/Workflow action timed out/);assert.match(workflow,/criticalFailure/);assert.match(workflow,/existing\.status==="started"\?202/);assert.match(workflow,/status==="failed"\?409/);assert.match(workflow,/\.eq\("owner_id",user\.id\)/);assert.match(workflow,/dryRun=body\?\.dryRun===true/);assert.match(workflow,/status:"simulated"/);assert.match(workflow,/No customer data was saved and no external message/i);assert.match(workflowPage,/dryRun:true/);
assert.match(checkout,/monetization_offers/);assert.match(checkout,/\.eq\("owner_id",user\.id\)/);assert.match(checkout,/Offer amount is outside the supported range/);assert.match(checkout,/Offer currency is invalid/);assert.match(checkout,/Offer billing mode is invalid/);assert.match(checkout,/idempotencyKey/);assert.match(integrations,/Idempotency-Key/);assert.match(integrations,/External provider timed out/);assert.match(integrations,/incomplete checkout session/);
assert.match(storePublish,/current_version_id/);assert.match(storePublish,/assessBuildQuality/);assert.match(storePublish,/evaluateReleaseReadiness/);assert.match(storePublish,/customer_approved_at/);assert.match(storePublish,/approved store listing must match the exact current project version/i);
assert.match(database,/SECRET_FIELD/);assert.match(database,/SAFE_TYPES/);assert.match(database,/_history/);assert.match(database,/owner-scoped by default/);assert.match(databaseRollback,/Restored as a new version|restored as a new version/i);assert.match(databaseRollback,/\.eq\("owner_id",user\.id\)/);assert.match(projectRollback,/Rollback to version/);assert.match(projectRollback,/\.eq\("owner_id", user\.id\)/);
assert.match(dataRecords,/auth\.getUser\(\)/);assert.match(dataRecords,/\.eq\("owner_id",user\.id\)|\.eq\("owner_id",ctx\.user\.id\)/);assert.match(dataRecords,/MAX_FIELDS = 24/);assert.match(dataRecords,/MAX_VALUE = 2000/);assert.match(dataRecords,/app_data_records/);assert.match(dataRecords,/export async function PATCH/);assert.match(dataRecords,/expectedUpdatedAt/);assert.match(dataRecords,/changed since it was loaded/);assert.match(dataRecordsMigration,/enable row level security/);assert.match(dataRecordsMigration,/revoke all on public\.app_data_records from public, anon, authenticated/);assert.match(dataRecordsMigration,/owner_id = \(select auth\.uid\(\)\)/);assert.match(generatedApp,/\/records\?entity=/);assert.match(generatedApp,/Saved securely to this project/);assert.match(generatedApp,/Private preview storage on this device/);
assert.match(videoCompile,/getVideoRendererConfig/);assert.match(videoCompile,/const initialRenderStatus=rendererConfigured\?"queued":"draft"/);assert.match(videoCompile,/startVideoRender\(\{project,version,editJson\}\)/);assert.match(videoCompile,/renderStarted:true/);assert.match(videoCompile,/saved safely as a draft/);assert.match(videoCompile,/invalid trim range/);assert.match(videoGateway,/VIDEO_RENDER_COST_POLICY_BLOCKED/);assert.match(videoGateway,/VIDEO_RENDER_INVALID_RESPONSE/);assert.match(videoGateway,/assertRuntimeUrlAllowed/);assert.match(videoGateway,/jobId/);assert.match(videoGateway,/outputPath/);assert.match(videoStatus,/checkVideoRenderStatus/);assert.match(videoStatus,/render_status/);assert.match(videoStatus,/output_path/);
assert.match(imageApi,/generateExternalImages/);assert.match(imageApi,/gateway\.configured/);assert.match(imageApi,/not presented as photorealistic external-model output/);assert.match(imageGateway,/IMAGE_GENERATION_COST_POLICY_BLOCKED/);assert.match(imageGateway,/IMAGE_GENERATION_INVALID_RESPONSE/);assert.match(imageGateway,/assertRuntimeUrlAllowed/);
for(const name of ['server_consume_app_builder_entitlement','server_consume_ai_credits','server_refund_ai_credits'])assert.match(finance,new RegExp(name));assert.match(generate,/consumeAppBuilderEntitlement\(user\.id/);assert.match(generate,/consumeAiCredits\(user\.id/);assert.match(generate,/bindAppBuilderProjectAccess\(user\.id/);assert.match(generate,/restoreFailedAppBuilderCreate\(userId/);assert.match(modify,/consumeAppBuilderEntitlement\(user\.id/);assert.match(modify,/consumeAiCredits\(user\.id/);assert.match(modify,/refundAiCredits\(userId/);assert.match(serverRpc,/Another creation request is already in progress/);assert.match(serverRpc,/app_builder_project_access/);assert.doesNotMatch(serverRpc,/first_app_id|first_app_published/);assert.match(serverRpc,/source_request_id/);assert.match(serverRpc,/Refund amount does not match original charge/);assert.match(serverRpc,/type='ai_refund'/);
assert.match(modify,/current_version_id/);assert.match(modify,/Current project version could not be loaded safely/);assert.match(modify,/withTimeout\(generateWithFallback\(prompt\),PRIMARY_AI_TIMEOUT_MS/);assert.match(modify,/withTimeout\(generateWithFallback\(repairPrompt\),REPAIR_AI_TIMEOUT_MS/);assert.match(modify,/server_save_app_modification/);assert.match(modify,/Project changed during modification/);assert.doesNotMatch(proAssistant,/specification:\s*initialSpec/);assert.match(proAssistant,/inFlightRef\.current/);assert.match(proAssistant,/fetchWithTimeout/);assert.match(proAssistant,/,95000\)/);assert.match(proAssistant,/expectedVersionId:data\?\.version\?\.id/);assert.match(proPage,/select\("id,version_no"\)\.eq\("id",app\.current_version_id\)/);assert.doesNotMatch(proPage,/select\("id,version_no,specification"\)/);assert.match(bootstrap,/expectedVersionId/);assert.match(bootstrap,/plan\?\.modules\?\.media/);assert.match(bootstrap,/\.limit\(40\)/);assert.match(modifyRuntime,/app_versions_app_request_unique/);assert.match(modifyRuntime,/server_save_app_modification/);assert.match(modifyRuntime,/for update/);assert.match(modifyRuntime,/Project changed during modification/);assert.match(modifyRuntime,/revoke all on function public\.server_save_app_modification/);assert.match(modifyRuntime,/to service_role/);
console.log('✓ Workflow execution is idempotent, bounded, fail-closed and supports side-effect-free Safe Test');
console.log('✓ Payment checkout uses authoritative offer data, validation, provider timeouts and idempotency');
console.log('✓ Store publish requests require exact-version approval plus the shared 100-point quality gate');
console.log('✓ No-code database/project rollback preserves history and ownership');
console.log('✓ Generated App records persist through an owner-scoped RLS runtime with bounded fields, safe editing concurrency and device fallback');
console.log('✓ Video compile stays draft without a renderer, requires a real job/output when connected and tracks final render status');
console.log('✓ Image generation uses a cost-gated HTTPS runtime with an explicit non-photorealistic local fallback');
console.log('✓ Creation entitlements and AI credits use server-only, request-bound, exact-project financial runtime');
console.log('✓ Professional AI modify is time-bounded, duplicate-click guarded, canonical-version based and concurrency safe');
console.log('✓ Professional module sync rejects stale versions and bounds media work');
