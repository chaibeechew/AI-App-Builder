import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();
const read=(p)=>fs.existsSync(path.join(root,p))?fs.readFileSync(path.join(root,p),'utf8'):'';
const exists=(p)=>fs.existsSync(path.join(root,p));

const files={
  readiness:read('lib/non-production-readiness.js'), policy:read('config/product-policy.js'), visualPolicy:read('lib/ai/premium-visual-policy.js'), buildStandards:read('lib/buildStandards.js'), wallpapers:read('lib/design/wallpaper-presets.js'), wallpaperEngine:read('app/components/AdaptiveWallpaperEngine.js'), imageStudio:read('app/image-studio/page.js'), imageApi:read('app/api/images/generate/route.js'), home:read('app/page.js'), create:read('app/create/page.js'), generate:read('app/api/generate/route.js'), modify:read('app/api/modify/route.js'), editor:read('app/editor/[id]/page.js'), generatedApp:read('app/a/[id]/GeneratedAppClient.js'), generatedWebsite:read('app/website/[id]/page.js'), projectMemory:read('lib/project-memory.js'), dashboard:read('app/app-dashboard/[id]/page.js'), pro:read('app/pro/[id]/page.js'), proAssistant:read('app/pro/[id]/ProAssistant.js'), proModePolicy:read('lib/pro-mode.js'), accessReader:read('lib/app-builder-access.js'), accessRuntime:read('supabase/migrations/20260831120000_preview_access_credit_runtime.sql'), dataPage:read('app/database/[id]/page.js'), connectionsPage:read('app/integrations/[id]/page.js'), paymentsPage:read('app/monetization/[id]/page.js'), workflow:read('app/api/apps/[id]/workflows/[workflowId]/run/route.js'), database:read('app/api/apps/[id]/database/route.js'), checkout:read('app/api/apps/[id]/monetization/[offerId]/checkout/route.js'), video:read('app/api/video/projects/[id]/compile/route.js'), orchestrator:read('lib/build/orchestrator.js'), publish:read('app/publish/[id]/page.js'), metadata:read('app/api/store-metadata/route.js'), metadataSave:read('app/api/store-metadata/save/route.js'), publishingAgent:read('app/api/apps/[id]/publishing-agent/route.js'), layout:read('app/layout.js'), productCopy:read('app/components/ProductCopyFix.js'), studio:read('app/studio/page.js'),
};

const requiredAreas=['generation','editing','data','automation','publishing','security','reliability','visual','wallpaper','imageStudio','versioning','pro','branding'];
assert.match(files.readiness,/NON_PRODUCTION_SCORE_REQUIRED = 100/);for(const key of requiredAreas)assert.match(files.readiness,new RegExp(`key: "${key}"`),`Missing readiness area: ${key}`);assert.match(files.readiness,/productionHeld: true/);assert.match(files.policy,/productionPromotionHold: true/);assert.match(files.policy,/explicitApprovalRequiredBeforeProduction: true/);

assert.match(files.policy,/product: "AI BUILD APP & WEB"/);assert.match(files.layout,/PRODUCT_BRAND\.name/);assert.match(files.productCopy,/AI BUILD APP & WEB/);assert.match(files.studio,/AI BUILD APP & WEB/);assert.doesNotMatch(files.studio,/3,000\+/);
assert.match(files.home,/AI BUILD APP & WEB/);assert.match(files.home,/First project free until publish/);assert.match(files.home,/external store fees stay separate/);assert.match(files.home,/appId,requestId:newRequestId\("modify"\)/);assert.match(files.home,/versionId:d\?\.version\?\.id/);assert.match(files.home,/Production stays locked until approved/);
assert.match(files.create,/requestId:requestId\("create"\)/);assert.match(files.create,/high_performance_desktop/);assert.doesNotMatch(files.create,/desktop_pro/);assert.match(files.create,/not trademark clearance/i);assert.match(files.create,/customerVisualPreferences/);assert.match(files.create,/wallpaperMode/);

assert.match(files.visualPolicy,/customer color preference is authoritative/i);assert.match(files.visualPolicy,/WALLPAPER & STEP VISUALS/);assert.match(files.wallpapers,/WALLPAPER_PRESETS/);assert.match(files.wallpapers,/pickWallpaperForStage/);assert.match(files.wallpaperEngine,/MutationObserver/);assert.match(files.wallpaperEngine,/AI Random/);assert.match(files.wallpaperEngine,/localStorage\.setItem\(STORAGE_KEY/);assert.match(files.wallpaperEngine,/ai_build_wallpaper/);assert.match(files.wallpaperEngine,/textOf\("\.progress"\)/,'Home build progress must drive exact wallpaper stages.');assert.match(files.wallpaperEngine,/ai-build-stage-change/,'Explicit build stage events must be supported.');
assert.match(files.editor,/COLOR & WALLPAPER/);assert.match(files.editor,/type="color"/);assert.match(files.editor,/WALLPAPER_PRESETS/);assert.match(files.projectMemory,/visualPreferences/);assert.match(files.generate,/wallpaperMode/);assert.match(files.generate,/visual_preferences/);assert.match(files.generate,/ai_build_wallpaper/);assert.match(files.modify,/PREMIUM_VISUAL_AI_INSTRUCTION/);assert.match(files.modify,/visualPreferences/);
assert.match(files.generatedApp,/wallpaperStyle/);assert.match(files.generatedApp,/var\(--surface\)/,'Generated App cards must use the customer surface color.');assert.match(files.generatedApp,/var\(--foreground\)/,'Generated App text must use the customer text color.');assert.doesNotMatch(files.generatedApp,/background:#fffffff0/,'Generated App must not force white cards over a customer theme.');
assert.match(files.generatedWebsite,/wallpaperStyle/);assert.match(files.generatedWebsite,/"--surface":surface/,'Generated Website must expose the saved customer surface color.');assert.match(files.generatedWebsite,/background:var\(--surface\)/,'Generated Website content/cards must use the saved surface color.');assert.match(files.generatedWebsite,/color:var\(--foreground\)/,'Generated Website must coordinate saved foreground color.');
assert.match(files.buildStandards,/BEAUTY_EVIDENCE_FIELDS/);assert.match(files.buildStandards,/wallpaperPreset/);assert.match(files.buildStandards,/paletteRationale/);assert.match(files.buildStandards,/cardStyle/);assert.match(files.buildStandards,/imageStyle/);

assert.match(files.imageStudio,/Design Images/);assert.match(files.imageStudio,/Style Tools/);assert.match(files.imageStudio,/Templates/);assert.match(files.imageStudio,/Use as Wallpaper/);assert.match(files.imageApi,/images/);assert.match(files.imageApi,/Soolen Visual Engine/);assert.match(files.imageApi,/not presented as photorealistic external-model output/);

assert.ok(exists('app/pro/[id]/page.js')&&exists('app/pro/[id]/ProAssistant.js'));assert.match(files.proAssistant,/\/api\/modify/);assert.match(files.proAssistant,/requestId/);assert.match(files.proAssistant,/buildAutonomousPlan/);assert.match(files.pro,/getAppBuilderAccess/);assert.match(files.pro,/!access\.professional\.active/);assert.match(files.pro,/US\$68 · 365 days · no auto-renew/);assert.match(files.proModePolicy,/professionalAutoRenew: false/);assert.match(files.proModePolicy,/priceReviewIntervalYears: 3/);assert.match(files.proModePolicy,/priceIncreaseOptional: true/);assert.match(files.accessReader,/pro_valid_until/);assert.match(files.accessRuntime,/standard_project_credits/);assert.match(files.accessRuntime,/grant_standard_project_credit/);assert.match(files.accessRuntime,/grant_pro_access/);assert.match(files.accessRuntime,/to service_role/);
assert.match(files.dataPage,/SOOLENAI · CUSTOMER DATA/);assert.match(files.dataPage,/Technical details hidden/);assert.match(files.connectionsPage,/SOOLENAI · CONNECTIONS/);assert.match(files.connectionsPage,/SECRETS STAY SERVER-SIDE/);assert.match(files.paymentsPage,/SOOLENAI · PAYMENTS & OFFERS/);assert.match(files.paymentsPage,/SERVER-CHECKED PRICE/);
assert.match(files.generate,/p_request_id:chargeRequestId/);assert.match(files.generate,/bind_app_builder_project_access/);assert.match(files.generate,/restore_failed_app_builder_create/);assert.match(files.modify,/p_request_id:chargeRequestId/);assert.match(files.accessRuntime,/Refund amount does not match original charge/);assert.match(files.accessRuntime,/credit_transactions_request_type_unique_idx/);
assert.match(files.workflow,/idempotencyKey/);assert.match(files.workflow,/status:"failed"/);assert.match(files.database,/No API keys, passwords or payment credentials/);assert.match(files.checkout,/idempotencyKey/);assert.match(files.checkout,/Offer amount is outside the supported range/);assert.match(files.video,/serverRender:true/);assert.match(files.video,/renderStarted:false/);assert.match(files.orchestrator,/type:"send_whatsapp"/);assert.doesNotMatch(files.orchestrator,/type:"whatsapp"/);
assert.match(files.metadata,/customerAnswers/);assert.match(files.metadata,/privacyPolicyUrl/);assert.match(files.metadata,/targetAudience/);assert.match(files.publish,/AI Auto-Fill Store Forms/);assert.match(files.publish,/AI PUBLISHING AGENT/);assert.match(files.publish,/Apple Developer Program/);assert.match(files.publish,/Google Play/);assert.match(files.publishingAgent,/readyForOfficialSubmission: false/);assert.match(files.metadataSave,/customer_approved_at: null/);
const dashboardHasPro=/\/pro\/\$\{id\}|\/pro\//.test(files.dashboard);assert.equal(dashboardHasPro,true);assert.match(files.dashboard,/getAppBuilderAccess/);assert.match(files.dashboard,/WORKSPACE MODE/);assert.match(files.dashboard,/STANDARD MODE · CURRENT/);assert.match(files.dashboard,/AI handles everything for you/);assert.match(files.dashboard,/same project and the same version history/i);

console.log('✓ Non-production 100-point contract covers every required product area');
console.log('✓ AI BUILD APP & WEB branding, premium whole-system colors and exact random-by-step wallpaper behavior are protected');
console.log('✓ Generated App + Website render saved customer surface, foreground and wallpaper choices');
console.log('✓ Image Studio, no-code editing, data, automation, payments, publishing preparation and Pro access contracts are protected');
console.log('✓ Production remains intentionally held');
