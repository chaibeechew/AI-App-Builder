import assert from "node:assert/strict";
import fs from "node:fs";
import { createCommunicationServiceRuntime, communicationServiceCapabilities, normalizeServiceMessage, planServiceMessage } from "../lib/communications/service-core.js";
import { signServiceRequest, verifyServiceRequestSignature } from "../lib/communications/service-auth.js";
const core=fs.readFileSync("lib/communications/service-core.js","utf8"),runtime=fs.readFileSync("lib/communications/runtime-port.js","utf8"),statusRoute=fs.readFileSync("app/api/communications/v1/status/route.js","utf8"),dispatchRoute=fs.readFileSync("app/api/communications/v1/dispatch/route.js","utf8"),requestStore=fs.readFileSync("lib/communications/service-request-store.js","utf8"),client=fs.readFileSync("lib/communications/service-client.js","utf8"),migration=fs.readFileSync("supabase/migrations/20260903155200_independent_communications_service_requests.sql","utf8"),docs=fs.readFileSync("docs/independent-communications-service.md","utf8");
for(const forbidden of ["next/","react","@supabase","vercel","app/"])assert.equal(core.includes(forbidden),false,`Independent service core must not depend on ${forbidden}`);
assert.match(core,/createCommunicationServiceRuntime/);assert.match(core,/deliverZeroCostCommunication/);assert.match(core,/idempotencyKey/);assert.match(runtime,/createEmbeddedCommunicationRuntime/);assert.match(statusRoute,/communicationServiceCapabilities/);assert.match(statusRoute,/Cache-Control/);assert.doesNotMatch(statusRoute,/process\.env\.[A-Z0-9_]*(TOKEN|SECRET|KEY)/);
assert.match(dispatchRoute,/verifyServiceRequestSignature/);assert.ok(dispatchRoute.indexOf("claim=await claimCommunicationServiceRequest")<dispatchRoute.indexOf("const result=await dispatchServiceMessage"));assert.match(dispatchRoute,/LANERIQ_COMMUNICATIONS_ALLOW_CUSTOMER_BILLED/);assert.ok(dispatchRoute.indexOf("message={...message,allowCustomerBilledProvider:false}")<dispatchRoute.indexOf("const result=await dispatchServiceMessage"));assert.match(dispatchRoute,/replay_blocked/);assert.match(dispatchRoute,/already_claimed/);
assert.match(requestStore,/server_claim_communication_service_request/);assert.match(migration,/communication_service_requests_nonce_uq/);assert.match(migration,/communication_service_requests_idempotency_uq/);assert.match(migration,/pg_advisory_xact_lock/);assert.match(migration,/auth\.role\(\).*service_role/);assert.match(migration,/revoke all on public\.communication_service_requests from public,anon,authenticated/);assert.match(client,/signServiceRequest/);assert.match(client,/redirect:"error"/);assert.match(client,/https:/);assert.match(docs,/No unauthenticated arbitrary-send endpoint/);
const runtimeFixture=createCommunicationServiceRuntime({adapterStatus:()=>({in_app:{contractReady:true,runtimeReady:true,providerReady:true,liveVerified:false,costClass:"free",health:"ready",evidenceLevel:"CODE"},sms:{contractReady:true,runtimeReady:true,providerReady:true,liveVerified:false,costClass:"paid",health:"ready",evidenceLevel:"CODE"}}),senders:()=>({in_app:async()=>({status:"completed"}),sms:async()=>({status:"completed"})})});
const capabilities=communicationServiceCapabilities(runtimeFixture);assert.equal(capabilities.architecture,"provider_agnostic_port_adapter");assert.equal(capabilities.channels.sms.costClass,"paid");
const input={idempotencyKey:"msg-test-0001",to:"user-1",body:"Hello",preferredChannels:["in_app","sms"]};const message=normalizeServiceMessage(input);assert.equal(message.idempotencyKey,"msg-test-0001");const planned=planServiceMessage(runtimeFixture,{...input,preferredChannels:["sms","in_app"]});assert.equal(planned.plan.selected,"in_app");assert.equal(planned.plan.externalSpendCap,0);
const secret="independent-communications-test-secret-1234567890",body=JSON.stringify(input),timestamp="1788450000",nonce="nonce_0123456789abcdef";const signature=signServiceRequest({secret,clientId:"laneriq-ai",timestamp,nonce,method:"POST",path:"/api/communications/v1/dispatch",body});assert.match(signature,/^[a-f0-9]{64}$/);assert.equal(verifyServiceRequestSignature({secret,signature,clientId:"laneriq-ai",timestamp,nonce,method:"POST",path:"/api/communications/v1/dispatch",body,nowMs:1788450000000}).ok,true);assert.equal(verifyServiceRequestSignature({secret,signature:"0".repeat(64),clientId:"laneriq-ai",timestamp,nonce,method:"POST",path:"/api/communications/v1/dispatch",body,nowMs:1788450000000}).ok,false);assert.equal(verifyServiceRequestSignature({secret,signature,clientId:"laneriq-ai",timestamp,nonce,method:"POST",path:"/api/communications/v1/dispatch",body,nowMs:1788451000000}).reason,"stale_request");
console.log("✓ Independent communications core + signed dispatch boundary");console.log("✓ Persistent nonce/idempotency replay protection is service-role-only");console.log("✓ Signed callers cannot bypass the RM0 gate by default");
