# Consolidated Preview Trigger

This file intentionally triggers a fresh preview deployment for the primary `ai-app-builder` Vercel project after disconnecting legacy projects from the same Git repository.
