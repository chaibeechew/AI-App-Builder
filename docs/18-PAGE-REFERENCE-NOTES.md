# Reference review notes

The user explicitly requires the approved screenshots to be matched for both layout and controls. The UI branch therefore treats the screenshot set as the visual authority for spacing, card composition, button shapes, state styling and navigation geometry.

This does not authorize fake production metrics or release claims. Visual fidelity and runtime truth are separate gates.
