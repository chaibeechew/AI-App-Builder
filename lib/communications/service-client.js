import crypto from "node:crypto";
import { signServiceRequest } from "./service-auth.js";
const DISPATCH_PATH="/api/communications/v1/dispatch"; const TIMEOUT_MS=8000;
function serviceUrl(value){const raw=String(value||"").trim();if(!raw)throw new Error("LANERIQ communication service URL is not configured.");const url=new URL(raw);const local=url.hostname==="localhost"||url.hostname==="127.0.0.1";if(url.protocol!=="https:"&&!local)throw new Error("LANERIQ communication service URL must use HTTPS.");url.pathname=DISPATCH_PATH;url.search="";url.hash="";return url;}
function serviceSecret(value){const secret=String(value||"").trim();if(secret.length<32)throw new Error("LANERIQ communication service secret is not configured.");return secret;}
function clientId(value){const id=String(value||"laneriq-ai").trim();if(!/^[A-Za-z0-9._:-]{1,180}$/.test(id))throw new Error("LANERIQ communication service client id is invalid.");return id;}
export function communicationServiceClientStatus(env=process.env){let ready=false;try{serviceUrl(env.LANERIQ_COMMUNICATIONS_SERVICE_URL);serviceSecret(env.LANERIQ_COMMUNICATIONS_SERVICE_SECRET);clientId(env.LANERIQ_COMMUNICATIONS_SERVICE_CLIENT_ID);ready=true;}catch{}return {mode:ready?"remote":"embedded",remoteReady:ready,evidenceLevel:"CODE"};}
export async function dispatchRemoteCommunication(message,{env=process.env,fetchImpl=fetch}={}){const url=serviceUrl(env.LANERIQ_COMMUNICATIONS_SERVICE_URL);const secret=serviceSecret(env.LANERIQ_COMMUNICATIONS_SERVICE_SECRET);const safeClient=clientId(env.LANERIQ_COMMUNICATIONS_SERVICE_CLIENT_ID);const body=JSON.stringify(message||{});if(Buffer.byteLength(body,"utf8")>65536)throw new Error("LANERIQ communication service request is too large.");const timestamp=String(Math.floor(Date.now()/1000));const nonce=crypto.randomBytes(18).toString("base64url");const signature=signServiceRequest({secret,clientId:safeClient,timestamp,nonce,method:"POST",path:DISPATCH_PATH,body});const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),TIMEOUT_MS);try{const response=await fetchImpl(url,{method:"POST",headers:{"content-type":"application/json","x-laneriq-client-id":safeClient,"x-laneriq-timestamp":timestamp,"x-laneriq-nonce":nonce,"x-laneriq-signature":signature},body,signal:controller.signal,redirect:"error",cache:"no-store"});const data=await response.json().catch(()=>({ok:false,error:"Invalid communications service response."}));if(!response.ok&&response.status!==202)throw new Error(String(data?.error||data?.status||`Communication service HTTP ${response.status}`).slice(0,240));return data;}finally{clearTimeout(timer);}}
