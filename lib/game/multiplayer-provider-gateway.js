import {assertRuntimeUrlAllowed} from "../soolen/security-policy.js";
import {getSoolenCostMode} from "../soolen/cost-policy.js";

const TIMEOUT_MS=12000;
const MAX_RESPONSE_BYTES=64*1024;
const ID=/^[A-Za-z0-9._:-]{1,240}$/;
const SAFE_STATUS=new Set(["searching","matched","cancelled","failed"]);

export class MultiplayerGatewayError extends Error{
  constructor(message,code="MULTIPLAYER_GATEWAY_ERROR",status=502){super(message);this.name="MultiplayerGatewayError";this.code=code;this.status=status;}
}
function text(v,max=240){return String(v??"").replace(/[\u0000-\u001f\u007f]/g,"").trim().slice(0,max);}
function costClass(v){const c=String(v||"metered").toLowerCase().trim();return["zero","free","metered"].includes(c)?c:"metered";}
function allowedCost(c,mode){if(mode==="paid"||mode==="balanced")return true;if(mode==="free")return c==="zero"||c==="free";return c==="zero";}
function endpoint(v,code){try{return assertRuntimeUrlAllowed(String(v||"").trim());}catch(e){throw new MultiplayerGatewayError("The multiplayer service endpoint is not allowed.",code,e?.status||500);}}
function headers(){const h={"Content-Type":"application/json",Accept:"application/json"};const token=String(process.env.MULTIPLAYER_PROVIDER_TOKEN||"").trim();if(token)h.Authorization=`Bearer ${token}`;return h;}
async function providerFetch(url,init,timeoutCode){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),TIMEOUT_MS);try{const response=await fetch(url,{...init,headers:headers(),redirect:"error",cache:"no-store",signal:controller.signal});const raw=await response.text();if(Buffer.byteLength(raw,"utf8")>MAX_RESPONSE_BYTES)throw new MultiplayerGatewayError("The multiplayer service returned an oversized response.","MULTIPLAYER_RESPONSE_TOO_LARGE",502);let data={};try{data=raw?JSON.parse(raw):{}}catch{}if(!response.ok)throw new MultiplayerGatewayError("The multiplayer service rejected the request.",text(data?.code,100)||"MULTIPLAYER_PROVIDER_REJECTED",response.status>=400&&response.status<600?response.status:502);return data;}catch(e){if(e instanceof MultiplayerGatewayError)throw e;if(e?.name==="AbortError")throw new MultiplayerGatewayError("The multiplayer service timed out.",timeoutCode,504);throw new MultiplayerGatewayError("The multiplayer service could not be reached.","MULTIPLAYER_PROVIDER_UNREACHABLE",503);}finally{clearTimeout(timer);}}
function normalizedStatus(value,fallback="searching"){const s=String(value||"").toLowerCase().trim();if(["queued","pending","searching"].includes(s))return"searching";if(["matched","found","ready"].includes(s))return"matched";if(["cancelled","canceled"].includes(s))return"cancelled";if(["failed","error","expired"].includes(s))return"failed";return SAFE_STATUS.has(s)?s:fallback;}
function validId(value){const v=text(value,240);return ID.test(v)?v:null;}

export function getMultiplayerProviderConfig(){const provider=text(process.env.MULTIPLAYER_PROVIDER,80),matchmaking=String(process.env.MULTIPLAYER_MATCHMAKING_ENDPOINT||"").trim(),status=String(process.env.MULTIPLAYER_STATUS_ENDPOINT||"").trim(),cancel=String(process.env.MULTIPLAYER_CANCEL_ENDPOINT||"").trim(),c=costClass(process.env.MULTIPLAYER_COST_CLASS),mode=getSoolenCostMode(),connected=Boolean(provider&&matchmaking);return{connected,configured:connected&&allowedCost(c,mode),blockedByCostPolicy:connected&&!allowedCost(c,mode),costClass:c,costMode:mode,matchmakingEndpoint:matchmaking||null,statusEndpoint:status||null,cancelEndpoint:cancel||null};}

export async function createMultiplayerTicket({requestId,appId,playerId,mode="default",region="auto",partySize=1,teamSize=5}={}){const config=getMultiplayerProviderConfig();if(config.blockedByCostPolicy)throw new MultiplayerGatewayError("Live multiplayer is blocked by the current zero-cost policy.","MULTIPLAYER_COST_POLICY_BLOCKED",403);if(!config.configured)throw new MultiplayerGatewayError("A live multiplayer provider is not connected.","LIVE_MULTIPLAYER_NOT_CONNECTED",503);const data=await providerFetch(endpoint(config.matchmakingEndpoint,"MULTIPLAYER_ENDPOINT_INVALID"),{method:"POST",body:JSON.stringify({schemaVersion:1,requestId:text(requestId,160),appId:text(appId,64),playerId:text(playerId,64),mode:text(mode,48)||"default",region:text(region,64)||"auto",partySize:Math.max(1,Math.min(10,Number(partySize)||1)),teamSize:Math.max(1,Math.min(10,Number(teamSize)||5)})},"MULTIPLAYER_MATCHMAKING_TIMEOUT");const ticketId=validId(data?.ticketId||data?.id);if(!ticketId)throw new MultiplayerGatewayError("The multiplayer service did not return a valid ticket id.","MULTIPLAYER_INVALID_RESPONSE",502);return{ticketId,status:normalizedStatus(data?.status),matchId:validId(data?.matchId),region:text(data?.region,64)||null};}

function templatedUrl(template,ticketId,code){const id=encodeURIComponent(validId(ticketId)||"");if(!id)throw new MultiplayerGatewayError("A valid multiplayer ticket id is required.","MULTIPLAYER_TICKET_ID_INVALID",400);const raw=String(template||"").trim();if(!raw)return null;return endpoint(raw.includes("{ticketId}")?raw.replaceAll("{ticketId}",id):`${raw}${raw.includes("?")?"&":"?"}ticketId=${id}`,code);}
export async function checkMultiplayerTicket(ticketId){const config=getMultiplayerProviderConfig();if(!config.configured||!config.statusEndpoint)throw new MultiplayerGatewayError("Live multiplayer status checking is not connected.","LIVE_MULTIPLAYER_STATUS_NOT_CONNECTED",503);const data=await providerFetch(templatedUrl(config.statusEndpoint,ticketId,"MULTIPLAYER_STATUS_ENDPOINT_INVALID"),{method:"GET"},"MULTIPLAYER_STATUS_TIMEOUT");return{ticketId:validId(ticketId),status:normalizedStatus(data?.status),matchId:validId(data?.matchId),region:text(data?.region,64)||null};}
export async function cancelMultiplayerTicket(ticketId){const config=getMultiplayerProviderConfig();if(!config.configured||!config.cancelEndpoint)throw new MultiplayerGatewayError("Live multiplayer cancellation is not connected.","LIVE_MULTIPLAYER_CANCEL_NOT_CONNECTED",503);await providerFetch(templatedUrl(config.cancelEndpoint,ticketId,"MULTIPLAYER_CANCEL_ENDPOINT_INVALID"),{method:"POST",body:JSON.stringify({ticketId:validId(ticketId)})},"MULTIPLAYER_CANCEL_TIMEOUT");return{ticketId:validId(ticketId),status:"cancelled"};}