// Converts a verified app specification into a minimal runnable source proposal.
// This is deterministic scaffolding; AI may extend it through Software Engineer Brain.
function esc(v){return JSON.stringify(String(v??""));}
function routePath(route="/"){const r=String(route||"/").replace(/^\/+|\/+$/g,"");return r?`app/${r}/page.js`:"app/page.js";}
function pageSource(page,appName){const title=page?.name||page?.title||appName||"App";const components=Array.isArray(page?.components)?page.components:[];return `export default function Page(){return <main style={{padding:24,fontFamily:"system-ui"}}><h1>${String(title).replace(/[<>]/g,"")}</h1>${components.map((c,i)=>`<section key={${i}}><h2>${String(c?.name||c?.type||`Section ${i+1}`).replace(/[<>]/g,"")}</h2></section>`).join("")}</main>}`;}
export function materializeAppSpecification(spec={}){const files=[];const name=spec.name||"Generated App";files.push({path:"package.json",content:JSON.stringify({name:"soolen-generated-app",private:true,scripts:{dev:"next dev",build:"next build",start:"next start"},dependencies:{next:"latest",react:"latest","react-dom":"latest"}},null,2)});files.push({path:"app/layout.js",content:`export const metadata={title:${esc(name)}};export default function RootLayout({children}){return <html><body>{children}</body></html>}`});for(const p of spec.pages||[])files.push({path:routePath(p.route),content:pageSource(p,name)});if(!(spec.pages||[]).some(p=>String(p.route||"/")==="/"))files.push({path:"app/page.js",content:pageSource({name},name)});return {files,framework:"nextjs",sourceType:"deterministic-scaffold",requiresSandbox:true};}
