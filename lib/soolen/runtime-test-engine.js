// Runtime/UI verification plan. Required checks must be returned explicitly by the isolated browser executor.
function safeRoute(v){const s=String(v||"/").split(/[?#]/)[0];return s.startsWith("/")&&!s.includes("..")?s:"/";}
export function createRuntimeTestPlan(spec={}){const pages=Array.isArray(spec.pages)?spec.pages:[],navigation=Array.isArray(spec.navigation)?spec.navigation:[],routes=[...new Set(pages.map(p=>safeRoute(p.route)))];if(!routes.includes("/"))routes.unshift("/");return {version:2,externalNetwork:false,checks:[{id:"boot",type:"runtime",required:true,expect:"application starts and reaches ready state"},...routes.map(route=>({id:`route:${route}`,type:"route",required:true,route,expect:"HTTP success and visible page content without fatal error"})),...navigation.map((n,i)=>({id:`nav:${i}`,type:"navigation",required:true,route:safeRoute(n.route||n.href),expect:"navigation target resolves inside isolated origin"})),{id:"console",type:"runtime",required:true,expect:"no uncaught exception or unhandled rejection"},{id:"hydration",type:"runtime",required:true,expect:"no fatal hydration failure"},{id:"external-network",type:"security",required:true,expect:"no unauthorized external network request"},{id:"security",type:"security",required:true,expect:"sandbox policy remains enforced"},{id:"privacy",type:"privacy",required:true,expect:"no private data leaves authorized boundary"}]};}
export function summarizeRuntimeTests(results=[],plan){if(!plan||!Array.isArray(plan.checks))return {passed:false,status:"runtime-plan-required",total:Array.isArray(results)?results.length:0,required:0,failed:0,missing:0,inconclusive:0,failures:[{id:"plan",error:"runtime-test-plan-required"}]};const safeResults=Array.isArray(results)?results.slice(0,1000):[],required=plan.checks.filter(x=>x.required!==false),byId=new Map(safeResults.filter(x=>x?.id).map(x=>[x.id,x])),missing=required.filter(x=>!byId.has(x.id)).map(x=>x.id),failed=required.filter(x=>byId.get(x.id)?.passed===false),inconclusive=required.filter(x=>byId.has(x.id)&&byId.get(x.id)?.passed!==true&&byId.get(x.id)?.passed!==false),failures=[...failed.map(x=>({id:x.id,error:String(byId.get(x.id)?.error||"runtime-test-failed").slice(0,500)})),...missing.map(id=>({id,error:"required-test-missing"})),...inconclusive.map(x=>({id:x.id,error:"test-inconclusive"}))];const passed=required.length>0&&failures.length===0&&required.every(x=>byId.get(x.id)?.passed===true);return {passed,status:passed?"runtime-verified":"runtime-blocked",total:safeResults.length,required:required.length,failed:failed.length,missing:missing.length,inconclusive:inconclusive.length,failures};}
