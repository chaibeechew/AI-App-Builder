// Soolen Code Creator / Software Engineer Brain.
// Creates proposed source changes; execution remains behind Sandbox + permission gates.
const SAFE_PATH=/^(app|components|lib|public|tests|styles|supabase)\/[A-Za-z0-9_./-]+$/;
const FORBIDDEN=[/\.\.\//,/\.env/i,/secrets?/i,/private[_-]?key/i,/node_modules\//];
export function validateCodeProposal(proposal={}){const files=Array.isArray(proposal.files)?proposal.files:[];const errors=[];if(!files.length)errors.push("NO_FILES");for(const f of files){const path=String(f?.path||"");if(!SAFE_PATH.test(path)||FORBIDDEN.some(r=>r.test(path)))errors.push(`UNSAFE_PATH:${path}`);if(typeof f?.content!=="string")errors.push(`INVALID_CONTENT:${path}`);if(String(f?.content||"").length>250000)errors.push(`FILE_TOO_LARGE:${path}`);}return {passed:errors.length===0,errors,files};}
export function createCodeCreationPlan({goal,existing=[],failures=[]}={}){return {mode:"self-coding",goal:String(goal||"").slice(0,8000),existing:existing.map(String).slice(0,500),failures:failures.map(String).slice(0,100),rules:{createMissingModules:true,preserveWorkingCode:true,smallestSafeRepair:true,noSelfGrantedPermissions:true,noSecrets:true,sandboxRequiredBeforeAcceptance:true},stages:["understand","design","create-or-edit","static-review","sandbox-build","runtime-test","critic","repair","retest","accept"]};}
export function acceptanceGate({proposal,staticReview,sandbox,security,privacy}={}){const checks={proposal:validateCodeProposal(proposal).passed,staticReview:staticReview?.passed===true,sandbox:sandbox?.passed===true,security:security?.passed===true,privacy:privacy?.passed===true};return {passed:Object.values(checks).every(Boolean),checks,status:Object.values(checks).every(Boolean)?"accepted":"rejected"};}
