import { CREATIVE_GENOME_AXES, CREATIVE_GENOME_FAMILIES } from './creative-genome-library.js';
export function estimateCreativeGenomeSpace(){let combinations=1n;const axes={};for(const [axis,values] of Object.entries(CREATIVE_GENOME_AXES)){axes[axis]=values.length;combinations*=BigInt(values.length);}const families=Object.keys(CREATIVE_GENOME_FAMILIES).length;return Object.freeze({schemaVersion:'creative-genome-space.v1',familyCount:families,axisCount:Object.keys(axes).length,axisCardinality:Object.freeze(axes),rawGeneCombinationsPerFamily:combinations.toString(),rawFamilyGeneCombinations:(combinations*BigInt(families)).toString(),note:'This is a discrete CODE-space estimate before compatibility constraints, prompts, continuous controls, references, seeds, provider limits or real-output quality gates. It is not a count of stored templates.',truth:'CODE_READY'});}
